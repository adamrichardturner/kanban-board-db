# PostgreSQL Configuration
POSTGRES_USER=kanban_user
POSTGRES_PASSWORD=change_this_secure_password
POSTGRES_DB=kanban_board

# Database URL for external connections
DATABASE_URL=postgresql://kanban_user:change_this_secure_password@localhost:5432/kanban_board

# Server Configuration (optional)
POSTGRES_HOST=0.0.0.0
POSTGRES_PORT=5432

# Environment
NODE_ENV=production